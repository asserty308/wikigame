import 'package:flutter/material.dart';

class ClickGuessPage extends StatelessWidget {
  const ClickGuessPage({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(),
  );
}
