import 'package:flutter/material.dart';

class TimeTrialPage extends StatelessWidget {
  const TimeTrialPage({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(),
  );
}
