import 'package:flutter/material.dart';

class FiveToJesusPage extends StatelessWidget {
  const FiveToJesusPage({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(),
  );
}
